{
  "name": "@xframe/github-runner",
  "private": true,
  "version": "1.0.0",
  "description": "Dependency-free GitHub Actions JIT self-hosted runner listener",
  "type": "module",
  "engines": {
    "node": ">=20.6.0"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test test/*.test.js",
    "smoke": "node dist/github-runner.min.js --help"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
